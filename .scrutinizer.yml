filter:
    excluded_paths:
        - 'vendor/*'
        - 'js/jquery*'
        - 'js/handlebars*'
        - 'js/backbone*'
        - 'l10n/*'

imports:
    - php

tools:
    external_code_coverage:
        timeout: 3600 # Timeout in seconds. 60 minutes
